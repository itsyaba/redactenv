{
  "name": "redactenv-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Secret redaction package suite for Node.js servers",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @redactenv/core && npm run build -w @redactenv/patterns && npm run build -w @redactenv/logger -w @redactenv/express -w @redactenv/next",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/node_modules examples/*/node_modules node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsup": "^8.5.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
